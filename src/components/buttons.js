import React from 'react';
import Button from './button';

const Buttons = () => {
    return (
        <div className="buttons">
            <Button />
        </div>
    )
}

export default Buttons;